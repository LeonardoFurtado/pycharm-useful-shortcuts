# Duplicate until four lines (CTRL + D) and delete one (ctrl + y)
print("print one time")
# select the last word (ctrol + w), rename and select complete string (ctrl + w 2 times)
# move above line to the first ( ctrol shift up)
# use ctrl left | right to go to first string and ctrl w to change
